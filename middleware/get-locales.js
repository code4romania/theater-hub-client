
export default ({ store, redirect }) => {
    // if (store.getters['applicationData/locales'].length === 0) {
    //     return store.dispatch('applicationData/getLocales').then(() => {
    //         store.dispatch('setLocale', store.getters['applicationData/locales'][0].ID);
    //     });
    // }
}
